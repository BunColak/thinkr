<%= form_for(@thought) do |f| %>
<%= render 'shared/error_messages', object: f.object %>
<div class="field">
  <%= f.text_area :content, placeholder: "Tell people what you think..." %>
</div>
<%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
